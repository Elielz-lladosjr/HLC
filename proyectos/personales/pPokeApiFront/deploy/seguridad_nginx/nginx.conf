server {
    listen 80;
    server_name localhost;
    root /var/www/html;
    index index.html;

    auth_basic "Acceso Restringido - Proyecto Eliel";
    auth_basic_user_file /etc/nginx/.htpasswd;

    location / {
        try_files $uri $uri/ /index.html;
    }
}